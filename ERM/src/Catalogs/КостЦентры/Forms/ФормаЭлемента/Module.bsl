
&НаКлиенте
Процедура SubSegmentНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	// Прежде, чем выбирать саб сегмент - нужно выбрать сегмент
	Если НЕ ЗначениеЗаполнено(Объект.Segment) И Не (Объект.Source <> OracleMi Или Объект.Source <> OracleSmith) Тогда
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
			"""Segment"" is empty!",
			, "Segment", "Объект");
			
		СтандартнаяОбработка = Ложь;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура SubSubSegmentНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	// Прежде, чем выбирать саб саб сегмент нужно выбрать саб сегмент
	Если НЕ ЗначениеЗаполнено(Объект.SubSegment) И Не (Объект.Source <> OracleMi Или Объект.Source <> OracleSmith) Тогда
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
			"""Sub segment"" is empty!",
			, "SubSegment", "Объект");
			
		СтандартнаяОбработка = Ложь;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	OracleSmith = Перечисления.ТипыСоответствий.OracleSmith;
	OracleMi = Перечисления.ТипыСоответствий.OracleMI;
	
КонецПроцедуры
