
&НаСервере
Процедура ЗаполнитьСтатусыНаСервере()
	
	//ДатаОстатков = КонецДня(ДатаОстатков);
	//ДатаКомментария = НачалоДня(ДатаКомментария);
	//Обработки.ОбновлениеСтатусаInvoiceNotDue.ЗаполнитьСтатусInvoiceNotDue(ДатаОстатков, ДатаКомментария);
	Обработки.ОбновлениеСтатусаInvoiceNotDue.ЗаполнитьСтатусInvoiceNotDue();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьСтатусы(Команда)
	ЗаполнитьСтатусыНаСервере();
КонецПроцедуры
