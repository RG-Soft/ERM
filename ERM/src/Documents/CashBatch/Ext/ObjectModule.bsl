
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если Не ЭтоНовый() Тогда
		ДополнительныеСвойства.Вставить("СтарыеЗначенияКлючей", ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, "Номер"));
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ДополнительныеСвойства.Свойство("СтарыеЗначенияКлючей") Тогда
		
		СтарыеЗначенияКлючей = ДополнительныеСвойства.СтарыеЗначенияКлючей;
		Если СтарыеЗначенияКлючей.Номер <> Номер Тогда
			
			НЗ = РегистрыСведений.КлючиCashBatch.СоздатьНаборЗаписей();
			НЗ.Отбор.ArBatchNbr.Установить(СтарыеЗначенияКлючей.Номер);
			НЗ.Записать(Истина);
			
			НЗ.Отбор.ArInvoice.Установить(Номер);
			ЗаписьНабора = НЗ.Добавить();
			ЗаписьНабора.ArBatchNbr = Номер;
			ЗаписьНабора.CashBatch = Ссылка;
			НЗ.Записать(Истина);
			
		КонецЕсли;
		
	Иначе
		
		НЗ = РегистрыСведений.КлючиCashBatch.СоздатьНаборЗаписей();
		НЗ.Отбор.ArBatchNbr.Установить(Номер);
		ЗаписьНабора = НЗ.Добавить();
		ЗаписьНабора.ArBatchNbr = Номер;
		ЗаписьНабора.CashBatch = Ссылка;
		НЗ.Записать(Истина);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередУдалением(Отказ)
	
	НЗ = РегистрыСведений.КлючиCashBatch.СоздатьНаборЗаписей();
	НЗ.Отбор.ArBatchNbr.Установить(Номер);
	НЗ.Записать(Истина);
	
КонецПроцедуры
