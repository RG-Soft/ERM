
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ЗначениеЗаполнено(Объект.Currency) Тогда
		Элементы.Currency.Доступность = Ложь;
	Иначе
		Элементы.Currency.Доступность = Истина;
	КонецЕсли;
КонецПроцедуры
